language: node_js

node_js:
    - node

os:
    - linux
    - osx

script:
    - yarn build
    - yarn setup
    - figmagic
    - \[ -d figma \]
    - \[ -f figma/figma.js \]
    - \[ -d tokens \]
    - \[ -f tokens/colors.js \]

notifications:
    email: false
